import { registerButton } from "./state";

/** Represents the control for the "Left" mouse button. */
export const leftButton = registerButton("left");

/** Represents the control for the "Middle" mouse button. */
export const middleButton = registerButton("middle");

/** Represents the control for the "Right" mouse button. */
export const rightButton = registerButton("right");

/** Represents the control for the "Back" mouse button. */
export const backButton = registerButton("back");

/** Represents the control for the "Forward" mouse button. */
export const forwardButton = registerButton("forward");
