import { registerKey } from "./state";

/** Represents the key control for the `AltLeft` key. */
export const altLeftKey = registerKey("AltLeft");

/** Represents the key control for the `AltRight` key. */
export const altRightKey = registerKey("AltRight");

/** Represents the key control for the `ArrowDown` key. */
export const arrowDownKey = registerKey("ArrowDown");

/** Represents the key control for the `ArrowLeft` key. */
export const arrowLeftKey = registerKey("ArrowLeft");

/** Represents the key control for the `ArrowRight` key. */
export const arrowRightKey = registerKey("ArrowRight");

/** Represents the key control for the `ArrowUp` key. */
export const arrowUpKey = registerKey("ArrowUp");

/** Represents the key control for the `Backquote` key. */
export const backquoteKey = registerKey("Backquote");

/** Represents the key control for the `Backslash` key. */
export const backslashKey = registerKey("Backslash");

/** Represents the key control for the `Backspace` key. */
export const backspaceKey = registerKey("Backspace");

/** Represents the key control for the `BracketLeft` key. */
export const bracketLeftKey = registerKey("BracketLeft");

/** Represents the key control for the `BracketRight` key. */
export const bracketRightKey = registerKey("BracketRight");

/** Represents the key control for the `CapsLock` key. */
export const capsLockKey = registerKey("CapsLock");

/** Represents the key control for the `Comma` key. */
export const commaKey = registerKey("Comma");

/** Represents the key control for the `ContextMenu` key. */
export const contextMenuKey = registerKey("ContextMenu");

/** Represents the key control for the `ControlLeft` key. */
export const controlLeftKey = registerKey("ControlLeft");

/** Represents the key control for the `ControlRight` key. */
export const controlRightKey = registerKey("ControlRight");

/** Represents the key control for the `Copy` key. */
export const copyKey = registerKey("Copy");

/** Represents the key control for the `Cut` key. */
export const cutKey = registerKey("Cut");

/** Represents the key control for the `Delete` key. */
export const deleteKey = registerKey("Delete");

/** Represents the key control for the `Digit0` key. */
export const digit0Key = registerKey("Digit0");

/** Represents the key control for the `Digit1` key. */
export const digit1Key = registerKey("Digit1");

/** Represents the key control for the `Digit2` key. */
export const digit2Key = registerKey("Digit2");

/** Represents the key control for the `Digit3` key. */
export const digit3Key = registerKey("Digit3");

/** Represents the key control for the `Digit4` key. */
export const digit4Key = registerKey("Digit4");

/** Represents the key control for the `Digit5` key. */
export const digit5Key = registerKey("Digit5");

/** Represents the key control for the `Digit6` key. */
export const digit6Key = registerKey("Digit6");

/** Represents the key control for the `Digit7` key. */
export const digit7Key = registerKey("Digit7");

/** Represents the key control for the `Digit8` key. */
export const digit8Key = registerKey("Digit8");

/** Represents the key control for the `Digit9` key. */
export const digit9Key = registerKey("Digit9");

/** Represents the key control for the `End` key. */
export const endKey = registerKey("End");

/** Represents the key control for the `Enter` key. */
export const enterKey = registerKey("Enter");

/** Represents the key control for the `Equal` key. */
export const equalKey = registerKey("Equal");

/** Represents the key control for the `Escape` key. */
export const escapeKey = registerKey("Escape");

/** Represents the key control for the `F1` key. */
export const f1Key = registerKey("F1");

/** Represents the key control for the `F2` key. */
export const f2Key = registerKey("F2");

/** Represents the key control for the `F3` key. */
export const f3Key = registerKey("F3");

/** Represents the key control for the `F4` key. */
export const f4Key = registerKey("F4");

/** Represents the key control for the `F5` key. */
export const f5Key = registerKey("F5");

/** Represents the key control for the `F6` key. */
export const f6Key = registerKey("F6");

/** Represents the key control for the `F7` key. */
export const f7Key = registerKey("F7");

/** Represents the key control for the `F8` key. */
export const f8Key = registerKey("F8");

/** Represents the key control for the `F9` key. */
export const f9Key = registerKey("F9");

/** Represents the key control for the `F10` key. */
export const f10Key = registerKey("F10");

/** Represents the key control for the `F11` key. */
export const f11Key = registerKey("F11");

/** Represents the key control for the `F12` key. */
export const f12Key = registerKey("F12");

/** Represents the key control for the `F13` key. */
export const f13Key = registerKey("F13");

/** Represents the key control for the `F14` key. */
export const f14Key = registerKey("F14");

/** Represents the key control for the `F15` key. */
export const f15Key = registerKey("F15");

/** Represents the key control for the `F16` key. */
export const f16Key = registerKey("F16");

/** Represents the key control for the `F17` key. */
export const f17Key = registerKey("F17");

/** Represents the key control for the `F18` key. */
export const f18Key = registerKey("F18");

/** Represents the key control for the `F19` key. */
export const f19Key = registerKey("F19");

/** Represents the key control for the `F20` key. */
export const f20Key = registerKey("F20");

/** Represents the key control for the `F21` key. */
export const f21Key = registerKey("F21");

/** Represents the key control for the `F22` key. */
export const f22Key = registerKey("F22");

/** Represents the key control for the `F23` key. */
export const f23Key = registerKey("F23");

/** Represents the key control for the `F24` key. */
export const f24Key = registerKey("F24");

/** Represents the key control for the `Find` key. */
export const findKey = registerKey("Find");

/** Represents the key control for the `Fn` key. */
export const fnKey = registerKey("Fn");

/** Represents the key control for the `Help` key. */
export const helpKey = registerKey("Help");

/** Represents the key control for the `Home` key. */
export const homeKey = registerKey("Home");

/** Represents the key control for the `Insert` key. */
export const insertKey = registerKey("Insert");

/** Represents the key control for the `KeyA` key. */
export const aKey = registerKey("KeyA");

/** Represents the key control for the `KeyB` key. */
export const bKey = registerKey("KeyB");

/** Represents the key control for the `KeyC` key. */
export const cKey = registerKey("KeyC");

/** Represents the key control for the `KeyD` key. */
export const dKey = registerKey("KeyD");

/** Represents the key control for the `KeyE` key. */
export const eKey = registerKey("KeyE");

/** Represents the key control for the `KeyF` key. */
export const fKey = registerKey("KeyF");

/** Represents the key control for the `KeyG` key. */
export const gKey = registerKey("KeyG");

/** Represents the key control for the `KeyH` key. */
export const hKey = registerKey("KeyH");

/** Represents the key control for the `KeyI` key. */
export const iKey = registerKey("KeyI");

/** Represents the key control for the `KeyJ` key. */
export const jKey = registerKey("KeyJ");

/** Represents the key control for the `KeyK` key. */
export const kKey = registerKey("KeyK");

/** Represents the key control for the `KeyL` key. */
export const lKey = registerKey("KeyL");

/** Represents the key control for the `KeyM` key. */
export const mKey = registerKey("KeyM");

/** Represents the key control for the `KeyN` key. */
export const nKey = registerKey("KeyN");

/** Represents the key control for the `KeyO` key. */
export const oKey = registerKey("KeyO");

/** Represents the key control for the `KeyP` key. */
export const pKey = registerKey("KeyP");

/** Represents the key control for the `KeyQ` key. */
export const qKey = registerKey("KeyQ");

/** Represents the key control for the `KeyR` key. */
export const rKey = registerKey("KeyR");

/** Represents the key control for the `KeyS` key. */
export const sKey = registerKey("KeyS");

/** Represents the key control for the `KeyT` key. */
export const tKey = registerKey("KeyT");

/** Represents the key control for the `KeyU` key. */
export const uKey = registerKey("KeyU");

/** Represents the key control for the `KeyV` key. */
export const vKey = registerKey("KeyV");

/** Represents the key control for the `KeyW` key. */
export const wKey = registerKey("KeyW");

/** Represents the key control for the `KeyX` key. */
export const xKey = registerKey("KeyX");

/** Represents the key control for the `KeyY` key. */
export const yKey = registerKey("KeyY");

/** Represents the key control for the `KeyZ` key. */
export const zKey = registerKey("KeyZ");

/** Represents the key control for the `MetaLeft` key. */
export const metaLeftKey = registerKey("MetaLeft");

/** Represents the key control for the `MetaRight` key. */
export const metaRightKey = registerKey("MetaRight");

/** Represents the key control for the `Minus` key. */
export const minusKey = registerKey("Minus");

/** Represents the key control for the `NumLock` key. */
export const numLockKey = registerKey("NumLock");

/** Represents the key control for the `Numpad0` key. */
export const numpad0Key = registerKey("Numpad0");

/** Represents the key control for the `Numpad1` key. */
export const numpad1Key = registerKey("Numpad1");

/** Represents the key control for the `Numpad2` key. */
export const numpad2Key = registerKey("Numpad2");

/** Represents the key control for the `Numpad3` key. */
export const numpad3Key = registerKey("Numpad3");

/** Represents the key control for the `Numpad4` key. */
export const numpad4Key = registerKey("Numpad4");

/** Represents the key control for the `Numpad5` key. */
export const numpad5Key = registerKey("Numpad5");

/** Represents the key control for the `Numpad6` key. */
export const numpad6Key = registerKey("Numpad6");

/** Represents the key control for the `Numpad7` key. */
export const numpad7Key = registerKey("Numpad7");

/** Represents the key control for the `Numpad8` key. */
export const numpad8Key = registerKey("Numpad8");

/** Represents the key control for the `Numpad9` key. */
export const numpad9Key = registerKey("Numpad9");

/** Represents the key control for the `NumpadAdd` key. */
export const numpadAddKey = registerKey("NumpadAdd");

/** Represents the key control for the `NumpadDecimal` key. */
export const numpadDecimalKey = registerKey("NumpadDecimal");

/** Represents the key control for the `NumpadDivide` key. */
export const numpadDivideKey = registerKey("NumpadDivide");

/** Represents the key control for the `NumpadEnter` key. */
export const numpadEnterKey = registerKey("NumpadEnter");

/** Represents the key control for the `NumpadEqual` key. */
export const numpadEqualKey = registerKey("NumpadEqual");

/** Represents the key control for the `NumpadMultiply` key. */
export const numpadMultiplyKey = registerKey("NumpadMultiply");

/** Represents the key control for the `NumpadSubtract` key. */
export const numpadSubtractKey = registerKey("NumpadSubtract");

/** Represents the key control for the `OSLeft` key. */
export const osLeftKey = registerKey("OSLeft");

/** Represents the key control for the `OSRight` key. */
export const osRightKey = registerKey("OSRight");

/** Represents the key control for the `PageDown` key. */
export const pageDownKey = registerKey("PageDown");

/** Represents the key control for the `PageUp` key. */
export const pageUpKey = registerKey("PageUp");

/** Represents the key control for the `Paste` key. */
export const pasteKey = registerKey("Paste");

/** Represents the key control for the `Pause` key. */
export const pauseKey = registerKey("Pause");

/** Represents the key control for the `Period` key. */
export const periodKey = registerKey("Period");

/** Represents the key control for the `PrintScreen` key. */
export const printScreenKey = registerKey("PrintScreen");

/** Represents the key control for the `Props` key. */
export const propsKey = registerKey("Props");

/** Represents the key control for the `Quote` key. */
export const quoteKey = registerKey("Quote");

/** Represents the key control for the `ScrollLock` key. */
export const scrollLockKey = registerKey("ScrollLock");

/** Represents the key control for the `Semicolon` key. */
export const semicolonKey = registerKey("Semicolon");

/** Represents the key control for the `ShiftLeft` key. */
export const shiftLeftKey = registerKey("ShiftLeft");

/** Represents the key control for the `ShiftRight` key. */
export const shiftRightKey = registerKey("ShiftRight");

/** Represents the key control for the `Slash` key. */
export const slashKey = registerKey("Slash");

/** Represents the key control for the `Space` key. */
export const spaceKey = registerKey("Space");

/** Represents the key control for the `Tab` key. */
export const tabKey = registerKey("Tab");

/** Represents the key control for the `Undo` key. */
export const undoKey = registerKey("Undo");

/** Represents the key control for the `VolumeDown` key. */
export const volumeDownKey = registerKey("VolumeDown");

/** Represents the key control for the `VolumeMute` key. */
export const volumeMuteKey = registerKey("VolumeMute");

/** Represents the key control for the `VolumeUp` key. */
export const volumeUpKey = registerKey("VolumeUp");
